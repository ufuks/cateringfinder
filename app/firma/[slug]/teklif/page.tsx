'use client'; import {useState} from 'react'; import {Send, ShieldCheck} from 'lucide-react';
export default function QuoteRequest(){const [sent,setSent]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:f.get('name'),email:f.get('email'),phone:f.get('phone'),eventDate:f.get('eventDate'),eventType:f.get('eventType'),city:f.get('city'),people:f.get('people'),budget:f.get('budget'),notes:f.get('notes')})});if(r.ok)setSent(true)}return <main className="min-h-screen"><div className="container py-10 max-w-2xl"><div className="eyebrow">Güvenli teklif talebi</div><h1 className="text-4xl font-bold mt-2">Teklif Al</h1><p className="muted mt-3">Firma teklifini hazırlayabilsin diye etkinlik detaylarını paylaş.</p>{sent?<div className="card p-8 mt-8"><div className="w-12 h-12 rounded-full bg-cf-accent grid place-items-center"><ShieldCheck className="text-cf-primary"/></div><h2 className="text-2xl font-bold mt-5">Talebin gönderildi.</h2><p className="muted mt-2">Firma paneline yeni lead olarak düşecek. Teklif geldiğinde bildirim alacaksın.</p></div>:<form onSubmit={submit} className="card p-6 sm:p-8 mt-8 grid sm:grid-cols-2 gap-4">{[['name','Ad Soyad','text'],['phone','Telefon','tel'],['email','E-posta','email'],['eventDate','Etkinlik tarihi','date'],['eventType','Etkinlik türü','text'],['city','Şehir','text'],['people','Kişi sayısı','number'],['budget','Bütçe','number']].map(([n,l,t])=><label key={n}><span className="text-sm font-bold">{l}</span><input name={n} type={t} required className="mt-2 w-full h-12 border rounded-xl px-3"/></label>)}<label className="sm:col-span-2"><span className="text-sm font-bold">Not</span><textarea name="notes" rows={5} className="mt-2 w-full border rounded-xl p-3" placeholder="Menü, diyet, servis, kurulum gibi detaylar"/></label><label className="sm:col-span-2 flex gap-3 text-sm"><input type="checkbox" required/> KVKK aydınlatma metnini okudum.</label><button className="btn btn-primary sm:col-span-2"><Send size={17}/>Teklif Talebi Gönder</button></form>}</div></main>}
