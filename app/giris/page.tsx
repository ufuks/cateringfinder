'use client'; import {useState} from 'react'; import {LogIn} from 'lucide-react';
export default function Login(){const [msg,setMsg]=useState(''); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});const j=await r.json();if(r.ok) location.href=j.redirect; else setMsg(j.error)} return <main className="min-h-screen grid place-items-center p-6"><div className="w-full max-w-md"><a href="/" className="font-bold text-2xl text-cf-dark">CateFind</a><div className="card p-7 mt-6"><div className="eyebrow">Hoş geldin</div><h1 className="text-3xl font-bold mt-2">Giriş yap</h1><form onSubmit={submit} className="grid gap-4 mt-7"><input name="email" type="email" required placeholder="E-posta" className="h-12 border rounded-xl px-3"/><input name="password" type="password" required placeholder="Şifre" className="h-12 border rounded-xl px-3"/><button className="btn btn-primary"><LogIn size={17}/>Giriş Yap</button></form>{msg&&<p className="text-sm mt-4 text-red-700">{msg}</p>}<p className="text-sm muted mt-5">Hesabın yok mu? <a className="text-cf-primary font-bold" href="/kayit">Kayıt ol</a></p></div></div></main>}
