import {NextResponse} from 'next/server'; import {db} from '@/lib/db'; import {getSessionUser} from '@/lib/auth'; import {leadSchema} from '@/lib/validation';
export async function POST(req:Request){try{const user=await getSessionUser(); if(!user || user.role!=='CUSTOMER') return NextResponse.json({error:'Teklif talebi için müşteri hesabıyla giriş yapmalısınız.'},{status:401}); const body=leadSchema.parse(await req.json()); const lead=await db.lead.create({data:{customerId:user.id,title:`${body.eventType} · ${body.people} kişi`,eventDate:body.eventDate?new Date(body.eventDate):undefined,eventType:body.eventType,address:undefined,people:body.people,budgetMax:body.budget,notes:body.notes,requirements:{city:body.city,district:body.district,phone:body.phone,email:body.email,requesterName:body.name},status:'OPEN'}}); const companies=await db.company.findMany({where:{status:'ACTIVE',deletedAt:null},take:20,orderBy:[{rating:'desc'},{reviewCount:'desc'}]}); if(companies.length) await db.leadCompanyMatch.createMany({data:companies.map((c,i)=>({leadId:lead.id,companyId:c.id,score:Math.max(55,95-i*3)})),skipDuplicates:true}); return NextResponse.json({ok:true,leadId:lead.id})}catch{return NextResponse.json({error:'Talep bilgileri geçersiz.'},{status:400})}}
