'use client'; import {useState} from 'react'; import {useSearchParams} from 'next/navigation'; import {UserPlus} from 'lucide-react';
export default function Register(){const q=useSearchParams(); const [role,setRole]=useState<'CUSTOMER'|'COMPANY'>(q.get('role')==='company'?'COMPANY':'CUSTOMER'); const [msg,setMsg]=useState(''); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault(); const f=new FormData(e.currentTarget); const r=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:f.get('name'),email:f.get('email'),password:f.get('password'),role})}); const j=await r.json(); setMsg(j.message||j.error); if(r.ok) location.href=role==='COMPANY'?'/firma/panel':'/musteri/panel'} return <main className="min-h-screen grid place-items-center p-6"><div className="w-full max-w-md"><a href="/" className="font-bold text-2xl text-cf-dark">CateFind</a><div className="card p-7 mt-6"><div className="eyebrow">Kayıt</div><h1 className="text-3xl font-bold mt-2">Hesabını oluştur</h1><div className="grid grid-cols-2 gap-2 mt-6"><button type="button" onClick={()=>setRole('CUSTOMER')} className={`btn ${role==='CUSTOMER'?'btn-primary':'btn-secondary'}`}>Müşteri</button><button type="button" onClick={()=>setRole('COMPANY')} className={`btn ${role==='COMPANY'?'btn-primary':'btn-secondary'}`}>Firma</button></div><form onSubmit={submit} className="grid gap-4 mt-5"><input name="name" required placeholder="Ad Soyad / Firma yetkilisi" className="h-12 border rounded-xl px-3"/><input name="email" required type="email" placeholder="E-posta" className="h-12 border rounded-xl px-3"/><input name="password" required minLength={8} type="password" placeholder="En az 8 karakter" className="h-12 border rounded-xl px-3"/><button className="btn btn-primary"><UserPlus size={17}/>Kayıt Ol</button></form>{msg&&<p className="text-sm mt-4 text-cf-primary">{msg}</p>}<p className="text-sm muted mt-5">Zaten hesabın var mı? <a className="text-cf-primary font-bold" href="/giris">Giriş yap</a></p></div></div></main>}
